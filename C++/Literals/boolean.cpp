//A keyword denoting one of the two possible values of type bool.
//true == 1 //false == 0
bool b = true;
bool b = false;

//We can perform test with these values
if(b)
{
    //do this
}
else 
{
    //do this
}

