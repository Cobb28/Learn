/*

scanf() met a disposition les formats, qui indique que le type de la valeur que doit saisir l'utilisateur.
Les formats sont reconnaissables par l'utilisation du caractères % suivie par une suite de lettre,
appelée "indicateur de conversion".

*/


%c || char  //Uniquement pour récuperer un caractère et pas un nombre

%hdd & %hhi || signed char

%hd & %hi || short

%d & %i || int 

%f || float

%lf || double

%Lf || long double

%ld & %li || long 

%lld & lli || long long 

%hhu & %hhx & %hho || unsigned char

%hu & %hx & %ho || unsigned short

%u & %x & %o || unsigned int 

%lu & %lx & %lo || unsigned long

%llu & %llx & %llo || unsigned long long


/*

Il n’y a pas d’indicateur pour le type _Bool.

*/

